const colors=["postit-yellow","postit-pink","postit-green","postit-blue"];document.querySelectorAll(".card-body-faq").forEach(o=>{o.style.transform="rotate("+(10*Math.random()-5)+"deg)";let t=colors[Math.floor(Math.random()*colors.length)];o.classList.add(t)});